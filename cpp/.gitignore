# Local build outputs
build/

# Conan/CMake generated presets and env scripts (regenerated by `conan install` / `conan create`)
CMakeUserPresets.json
test_package/conan*.sh
test_package/deactivate_*.sh
